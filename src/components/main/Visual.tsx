import styled from "@emotion/styled";

const StyledVisual = styled.section`
    height: 100vh;
`;

const Visual = () => {
    return (
        <StyledVisual>
            
        </StyledVisual>
    );
}

export default Visual;